# レシピ管理アプリ 仕様書

## 1. アプリケーション概要

レシピを追加・管理できるWEBアプリケーション。レシピの名前、材料、作り方を登録し、分量調整機能を備えたレシピ管理システムを提供する。

## 2. 機能要件

### 2.1 レシピ名前登録

#### 必須項目
- **レシピ名**（テキスト、必須）
  - 最大文字数: 100文字
  - 空白のみの入力は不可

#### 任意項目
- **SVGアイコン**（画像、任意）
  - ファイル形式: SVG
  - 最大ファイルサイズ: 100KB
  - アップロードまたはプリセットから選択可能

### 2.2 材料登録

#### 2.2.1 材料構造タイプ

ユーザーは以下の2つの構造から選択可能：

**A. フラット構造**
- すべての材料を同一階層に登録
- 例：
  ```
  - 卵 2個
  - 砂糖 50g
  - 小麦粉 100g
  ```

**B. 階層構造**
- 最大3階層までの構造化された材料リスト
- グループ化により材料を整理
- 例：
  ```
  生地
    ├─ 卵 2個
    ├─ 砂糖 50g
    └─ 小麦粉 100g
  トッピング
    ├─ 生クリーム
    │   ├─ クリーム 200ml
    │   └─ 砂糖 20g
    └─ いちご 5個
  ```

#### 2.2.2 材料データモデル

各材料は以下の情報を持つ：

- **材料名**（テキスト、必須）
  - 最大文字数: 50文字
- **分量**（数値、任意）
  - 正の数値のみ
  - 小数点対応（例: 0.5）
- **単位**（テキスト、任意）
  - 例: g, ml, 個, 大さじ, 小さじ, カップ
  - プリセットから選択 or 自由入力
- **階層レベル**（1-3、階層構造の場合のみ）
- **親グループ**（階層構造の場合のみ）

### 2.3 分量調整モード

#### 2.3.1 概要
レシピの分量を柔軟に調整できる機能

#### 2.3.2 調整基準の選択

ユーザーは以下のいずれかを選択：

**A. 人数ベース**
- デフォルト人数を設定（例: 4人分）
- 希望人数に応じて全材料の分量を自動計算
- 計算式: `調整後分量 = 元の分量 × (希望人数 / 基準人数)`

**B. 特定材料ベース**
- 基準となる材料を1つ指定（例: 卵）
- その材料の分量を変更すると、他の材料も比例して調整
- 計算式: `調整後分量 = 元の分量 × (新しい基準材料分量 / 元の基準材料分量)`

**C. 倍率ベース**
- 0.5倍、2倍などの倍率を指定
- 全材料の分量を一律に倍率調整
- 計算式: `調整後分量 = 元の分量 × 倍率`

#### 2.3.3 調整時の表示
- 元の分量と調整後の分量を並べて表示
- 調整不可能な材料は明示（例: 「適量」「少々」などのテキストのみの材料）

### 2.4 レシピ作り方登録

#### 必須/任意
- 任意項目

#### 入力形式
- **マークダウン対応テキストエリア**
- または **ステップバイステップ形式**

**マークダウン形式**
- 自由なテキスト入力
- マークダウン記法サポート（見出し、リスト、太字など）

**ステップバイステップ形式**
- 各ステップを個別に登録
- ステップの並び替え可能
- 各ステップに以下を含む：
  - ステップ番号（自動採番）
  - 説明文（テキスト、必須）
  - 画像（任意）
  - 所要時間（分単位、任意）

## 3. データモデル設計

### 3.1 Recipe（レシピ）

```typescript
interface Recipe {
  id: string;
  name: string; // 必須
  icon?: string; // SVGファイルパスまたはBase64
  structureType: 'flat' | 'hierarchical'; // 材料構造タイプ
  ingredients: Ingredient[];
  scalingMode?: ScalingMode; // 分量調整設定
  instructions?: Instructions; // 作り方
  createdAt: Date;
  updatedAt: Date;
}
```

### 3.2 Ingredient（材料）

```typescript
interface Ingredient {
  id: string;
  name: string; // 必須
  amount?: number; // 分量（数値部分）
  unit?: string; // 単位
  level: 1 | 2 | 3; // 階層レベル（1が最上位）
  parentId?: string; // 親グループのID（階層構造の場合）
  order: number; // 表示順序
  isGroup: boolean; // グループかどうか（階層構造の場合）
}
```

### 3.3 ScalingMode（分量調整設定）

```typescript
interface ScalingMode {
  type: 'servings' | 'ingredient' | 'multiplier';
  baseServings?: number; // 人数ベースの場合の基準人数
  baseIngredientId?: string; // 特定材料ベースの場合の基準材料ID
  defaultMultiplier?: number; // デフォルト倍率（デフォルト: 1）
}
```

### 3.4 Instructions（作り方）

```typescript
interface Instructions {
  format: 'markdown' | 'steps';
  content?: string; // マークダウン形式の場合
  steps?: InstructionStep[]; // ステップ形式の場合
}

interface InstructionStep {
  id: string;
  order: number;
  description: string;
  image?: string; // 画像URL
  duration?: number; // 所要時間（分）
}
```

## 4. UI/UX要件

### 4.1 レシピ一覧画面
- カード形式でレシピを表示
- 各カードには: アイコン、レシピ名、材料数、作成日
- 検索・フィルター機能
- ソート機能（名前順、作成日順）

### 4.2 レシピ登録/編集画面

#### レイアウト
- ステップバイステップのウィザード形式
  1. 基本情報（名前、アイコン）
  2. 材料登録（構造タイプ選択、材料追加）
  3. 分量調整設定
  4. 作り方登録

または

- タブ形式
  - 基本情報タブ
  - 材料タブ
  - 作り方タブ

#### 材料登録UI（階層構造の場合）
- ドラッグ&ドロップでの階層変更
- インデント表示で階層を視覚化
- 「グループを追加」「材料を追加」ボタン
- 階層レベル3を超える場合は警告表示

### 4.3 レシピ詳細画面
- レシピ名とアイコンを大きく表示
- 材料リスト（階層構造を保持して表示）
- 分量調整コントロール
  - スライダーまたは入力フィールド
  - リアルタイムで分量を更新
- 作り方の表示（形式に応じて表示）

### 4.4 レスポンシブデザイン
- モバイル、タブレット、デスクトップ対応
- モバイルでは1カラム、デスクトップでは2-3カラムレイアウト

## 5. 技術スタック（提案）

### 5.1 フロントエンド
- **フレームワーク**: React 18+ または Next.js 14+
- **言語**: TypeScript
- **スタイリング**: Tailwind CSS または CSS Modules
- **状態管理**: React Context + Hooks または Zustand
- **フォーム管理**: React Hook Form + Zod（バリデーション）

### 5.2 バックエンド（オプション）

**オプションA: フルスタックNext.js**
- Next.js App Router
- API Routes
- データ永続化: JSON ファイル または SQLite

**オプションB: フロントエンドのみ**
- LocalStorage または IndexedDB でデータ保存
- 将来的にバックエンド追加可能な設計

### 5.3 開発ツール
- **パッケージマネージャー**: npm または pnpm
- **ビルドツール**: Vite または Next.js内蔵
- **リンター**: ESLint
- **フォーマッター**: Prettier

## 6. 非機能要件

### 6.1 パフォーマンス
- 初回ロード時間: 3秒以内
- 材料リストの描画: 100個まで滑らかに動作

### 6.2 ユーザビリティ
- 直感的なUI
- エラーメッセージは日本語で分かりやすく
- フォームバリデーションはリアルタイム

### 6.3 アクセシビリティ
- キーボード操作対応
- スクリーンリーダー対応（ARIA属性）
- 適切なコントラスト比

### 6.4 データの整合性
- 階層構造の深さ制限を強制（最大3階層）
- 分量調整時の数値オーバーフロー防止
- 必須項目の入力チェック

## 7. 将来的な拡張機能（検討中）

- レシピの共有機能
- お気に入り機能
- タグ付け・カテゴリ分類
- レシピの印刷機能
- 買い物リスト生成
- 栄養情報の表示
- レシピの検索（全文検索）
- レシピのインポート/エクスポート（JSON形式）

## 8. 開発フェーズ

### Phase 1: MVP（最小機能実装）
- レシピの登録（名前のみ）
- フラット構造の材料登録
- 基本的な分量調整（倍率ベース）
- シンプルな作り方登録（テキストのみ）

### Phase 2: 階層構造対応
- 階層構造の材料登録（最大3階層）
- 階層構造のUI実装
- ドラッグ&ドロップ機能

### Phase 3: 高度な機能
- SVGアイコンアップロード
- 複数の分量調整モード
- ステップバイステップの作り方
- 検索・フィルター機能

### Phase 4: 完成度向上
- レスポンシブデザインの最適化
- アクセシビリティ改善
- パフォーマンス最適化
- エラーハンドリング強化

---

**バージョン**: 1.0
**作成日**: 2025-11-22
**ステータス**: Draft
